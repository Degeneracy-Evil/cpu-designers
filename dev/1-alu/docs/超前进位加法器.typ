#import "tbook.typ"

#show: doc => tbook.tbook([32位超前进位加法器], doc)

= 32位超前进位加法器

== 数学原理

对每一位$i$，定义传播信号与生成信号：

$ p_i = a_i xor b_i $
$ g_i = a_i and b_i $

进位递推关系为：

$ c_(i+1) = g_i or (p_i and c_i) $

和位为：

$ s_i = p_i xor c_i $

若把$g_i, p_i$展开，可以把高位进位写成低位输入进位$c_0$与各级$g/p$的组合逻辑。例如4位块输出进位：

$ c_4 = g_3 or (p_3 g_2) or (p_3 p_2 g_1) or (p_3 p_2 p_1 g_0) or (p_3 p_2 p_1 p_0 c_0) $

这就是“超前进位”的核心：高位进位不必等待逐位串行传播，而可由与或网络直接得到（实际就是将行波进位加法器展开）。

== 实现要点

本工程的32位加法器采用分层模块化实现：

+ `cla_adder_4bit`：计算每位$g/p$、和位及块内进位链，输出4位结果。
+ `cla_adder_16bit`：由4个4位模块级联得到16位结果。
+ `cla_adder_32bit`：由2个16位模块级联得到32位结果。

在`rtl/cla_adder_32bit.v`中，关键连接如下：

+ 低16位加法器输入`cin`，输出中间进位`c15`。
+ 高16位加法器把`c15`作为输入进位，输出最终`cout`。
+ 两级模块的`sum`分别拼接为`sum[15:0]`与`sum[31:16]`。

这种实现方式的优势是结构清晰、易于复用和验证；代价是16位块之间仍有一级串行进位依赖。若后续追求更低延迟，可在16位或32位层引入组传播/组生成信号，进一步减少块间进位等待时间。

详细代码见`rtl/cla_adder_32bit.v`、`rtl/cla_adder_16bit.v`与`rtl/cla_adder_4bit.v`。

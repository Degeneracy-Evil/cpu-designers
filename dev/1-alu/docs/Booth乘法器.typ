#import "tbook.typ"

#show: doc => tbook.tbook([Booth乘法器], doc)

= Booth乘法器

== 数学原理

对于补码整数乘法，最直接的做法是按乘数每一位进行“部分积累加”，但这会产生较多加法操作。Booth编码通过观察相邻两位来压缩操作次数。

设当前考察位对为$(q_0, q_(-1))$，规则为：

+ $(0, 0)$或$(1, 1)$：不加不减。
+ $(0, 1)$：加被乘数$M$。
+ $(1, 0)$：减被乘数$M$。

等价地，可写成每轮对累加寄存器$A$做：

$ A arrow.l A + f(q_0, q_(-1), M) $

其中

$
  f = cases(
    0 \, & (q_0, q_(-1)) in \{00\, 11\},
    +M \, & (q_0, q_(-1)) = 01,
    -M \, & (q_0, q_(-1)) = 10,
  )
$

完成加/减后，对$\{A, Q, Q_(-1)\}$做一次算术右移，重复32轮后得到64位乘积。

== 实现要点

`rtl/booth_multiplier.v`采用三段状态：`IDLE -> COMPUTE -> FINISH`。

+ `IDLE`：等待`start`，并完成寄存器初始化：`A=0`，`Q=multiplier`，`Q_1=0`，`M=multiplicand`。
+ `COMPUTE`：执行32轮Booth迭代。
+ `FINISH`：拉高`done`一个周期后回到`IDLE`。

本实现把“加/减被乘数”统一为一次CLA加法：

+ 当位对为`01`时，`add_op_b=M`且`add_cin=0`，即$A+M$。
+ 当位对为`10`时，`add_op_b=~M`且`add_cin=1`，即$A+(~M+1)=A-M$。
+ 当位对为`00/11`时，`add_op_b=0`，后续右移等价于“本轮不改$A$再移位”。

右移更新遵循算术右移语义：

+ `A <= {A[31], A[31:1]}`或`{add_result[31], add_result[31:1]}`，最高位保持符号位。
+ `Q <= {A[0], Q[31:1]}`或`{add_result[0], Q[31:1]}`，把高半部最低位移入低半部最高位。
+ `Q_1 <= Q[0]`，用于下一轮构成Booth位对。

最终输出为`product={A,Q}`，实现32位有符号数乘法的64位结果。

详细代码见`rtl/booth_multiplier.v`。
